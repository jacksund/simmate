# -*- coding: utf-8 -*-

"""
This file is required!

You typically don't have anything extra other than what is shown below. All this
does is tell Simmate (and then django) where to find your code.
"""

from django.apps import AppConfig


class ExampleAppConfig(AppConfig):
    name = "example_app"
